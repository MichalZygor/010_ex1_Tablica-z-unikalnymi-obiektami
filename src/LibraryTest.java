public class LibraryTest {
    public static void main(String[] args) {
        Library[] libraries = new Library[3];
        Library.createLibrary(libraries);
        Library.showArray(libraries);
    }
}
